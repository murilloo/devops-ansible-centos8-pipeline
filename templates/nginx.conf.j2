server {
  listen {{ hostvars[inventory_hostname]['ansible_eth1']['ipv4']['address']}}:8080;
  server_name {{ hostvars[inventory_hostname]['ansible_eth1']['ipv4']['address']}};
  root /nginx;
  location / {
      autoindex on;
      autoindex_exact_size off;
  }
}
